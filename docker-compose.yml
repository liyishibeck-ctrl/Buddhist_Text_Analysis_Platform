services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: buddha-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: buddha_corpus
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - buddha-postgres-data:/var/lib/postgresql/data
      - ./scripts/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d buddha_corpus"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  buddha-postgres-data:
